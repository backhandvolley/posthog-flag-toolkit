pre-commit:
  parallel: true
  commands:
    lint-and-format:
      glob: "*.{ts,js,json}"
      run: npx biome check --write {staged_files} && git add {staged_files}

pre-push:
  commands:
    typecheck:
      run: npm run typecheck
